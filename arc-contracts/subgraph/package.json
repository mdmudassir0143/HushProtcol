{
  "name": "@bullet/subgraph",
  "version": "0.1.0",
  "private": true,
  "description": "The Graph subgraph for BulletPool + MerkleRootManager on Arc Testnet",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio hush-protocol",
    "create-local": "graph create --node http://127.0.0.1:8020/ hush-protocol",
    "deploy-local": "graph deploy --node http://127.0.0.1:8020/ --ipfs http://127.0.0.1:5001 hush-protocol"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.97.1",
    "@graphprotocol/graph-ts": "^0.37.0"
  }
}
